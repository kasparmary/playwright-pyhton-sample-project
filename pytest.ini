[pytest]
addopts = -rA --browser chromium --headed tests/test_login_validation.py
testpaths = tests
markers =
    smoke: Critical business flow tests
    regression: Regression test suit

# Logging during test runs
log_cli = true
log_cli_level = INFO